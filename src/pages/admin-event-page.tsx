import * as React from 'react'
import { Check, Copy, ExternalLink, Play, Square } from 'lucide-react'
import { Link, useNavigate, useParams } from 'react-router-dom'
import { sileo } from 'sileo'
import Button from '@/components/ui/button'
import Input from '@/components/ui/input'
import PageShell from '@/components/page-shell'
import PageTransition from '@/components/page-transition'
import Textarea from '@/components/ui/textarea'
import { EventFrame } from '@/components/event-frame'
import { api, getAdminToken } from '@/lib/api'
import { themeOptions, themeStyle, type EventTheme } from '@/lib/event-theme'

type EventData = { id: string; name: string; event_date: string | null; welcome_message_text: string | null; invitation_frame: string; theme: EventTheme; settings: { allowText: boolean; allowImages: boolean; allowVideos: boolean; allowAudio: boolean }; status: 'draft' | 'active' | 'closed'; access_code: string }
const blank = { name: '', eventDate: '', welcomeMessageText: '', invitationFrame: 'classic-letter', preset: 'lavender', allowText: true, allowImages: true, allowVideos: true, allowAudio: true }
const frames = [{ id: 'classic-letter', label: 'Carta' }, { id: 'floral', label: 'Flores' }, { id: 'gallery', label: 'Galería' }]

const AdminEventPage = () => {
  const { eventId } = useParams(); const navigate = useNavigate(); const token = getAdminToken(); const [form, setForm] = React.useState(blank); const [event, setEvent] = React.useState<EventData | null>(null); const [saving, setSaving] = React.useState(false); const [acting, setActing] = React.useState(false)
  React.useEffect(() => { if (!token) { navigate('/admin/login'); return } if (eventId) api<{ event: EventData }>(`/api/events/${eventId}`, { token: token ?? undefined }).then(({ event }) => { setEvent(event); setForm({ name: event.name, eventDate: event.event_date?.slice(0, 16) ?? '', welcomeMessageText: event.welcome_message_text ?? '', invitationFrame: event.invitation_frame, preset: event.theme.preset, ...event.settings }) }).catch((err: Error) => sileo.error({ title: 'No pudimos abrir el evento', description: err.message })) }, [eventId, navigate, token])
  const set = <K extends keyof typeof form>(key: K, value: (typeof form)[K]) => setForm((current) => ({ ...current, [key]: value }))
  const theme: EventTheme = { preset: form.preset, colors: {} }
  const payload = () => ({ name: form.name, eventDate: form.eventDate ? new Date(form.eventDate).toISOString() : null, welcomeMessageText: form.welcomeMessageText || null, invitationFrame: form.invitationFrame, theme, settings: { allowText: form.allowText, allowImages: form.allowImages, allowVideos: form.allowVideos, allowAudio: form.allowAudio } })
  const save = async () => { setSaving(true); try { const result = await api<{ event: EventData }>(eventId ? `/api/events/${eventId}` : '/api/events', { method: eventId ? 'PATCH' : 'POST', token: token ?? undefined, body: JSON.stringify(payload()) }); sileo.success({ title: eventId ? 'Cambios guardados' : 'Evento creado' }); if (!eventId) navigate(`/admin/events/${result.event.id}`); else setEvent(result.event) } catch (err) { sileo.error({ title: 'No pudimos guardar', description: err instanceof Error ? err.message : undefined }) } finally { setSaving(false) } }
  const toggleStatus = async () => { if (!eventId || !event) return; const action = event.status === 'active' ? 'close' : 'activate'; setActing(true); try { const result = await api<{ event: EventData }>(`/api/events/${eventId}/${action}`, { method: 'POST', token: token ?? undefined }); setEvent(result.event); sileo.success({ title: action === 'close' ? 'Evento cerrado' : 'Evento activado' }) } catch (err) { sileo.error({ title: 'No pudimos actualizar el estado', description: err instanceof Error ? err.message : undefined }) } finally { setActing(false) } }
  const copy = async () => { try { await navigator.clipboard.writeText(event?.access_code ?? ''); sileo.success({ title: 'Código copiado' }) } catch { sileo.error({ title: 'No pudimos copiar el código' }) } }
  return <PageShell><PageTransition><section style={themeStyle(theme)} className="mx-auto max-w-5xl"><div className="mb-6 flex items-center justify-between"><Link to="/admin" className="text-sm font-bold text-slate-500 hover:text-slate-900">← Todos los eventos</Link>{event && <span className="rounded-full bg-[var(--event-secondary)] px-3 py-1 text-xs font-extrabold text-[var(--event-primary)]">{event.status === 'active' ? 'Activo' : event.status === 'closed' ? 'Cerrado' : 'Borrador'}</span>}</div><div className="grid gap-6 lg:grid-cols-[1.4fr_.6fr]"><section className="rounded-[2rem] bg-white p-6 shadow-[0_20px_60px_-35px_rgba(35,25,70,.35)] sm:p-9"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-[var(--event-primary)]">Configuración</p><h1 className="mt-2 text-3xl font-extrabold text-slate-900">{eventId ? 'Personaliza tu evento' : 'Crea un evento'}</h1><div className="mt-8 grid gap-5 sm:grid-cols-2"><label className="sm:col-span-2 text-sm font-bold text-slate-700">Nombre<Input value={form.name} onChange={(e) => set('name', e.target.value)} className="mt-2" placeholder="Boda de Ana y Juan" /></label><label className="text-sm font-bold text-slate-700">Fecha<Input type="datetime-local" value={form.eventDate} onChange={(e) => set('eventDate', e.target.value)} className="mt-2" /></label><label className="sm:col-span-2 text-sm font-bold text-slate-700">Mensaje de bienvenida<Textarea value={form.welcomeMessageText} onChange={(e) => set('welcomeMessageText', e.target.value)} className="mt-2" placeholder="Unas palabras para tus invitados" /></label></div><div className="mt-8"><p className="text-sm font-bold text-slate-700">Tema completo</p><div className="mt-3 grid grid-cols-2 gap-2 sm:grid-cols-4">{themeOptions.map((preset) => <button key={preset} onClick={() => set('preset', preset)} className={`rounded-2xl border p-3 text-left text-sm font-bold capitalize transition ${form.preset === preset ? 'border-[var(--event-primary)] bg-[var(--event-secondary)] text-[var(--event-primary)]' : 'border-slate-200 bg-white text-slate-600 hover:border-slate-300'}`}><span className="mb-2 block h-5 w-5 rounded-full bg-[var(--event-primary)]" />{preset}</button>)}</div></div><div className="mt-8"><p className="text-sm font-bold text-slate-700">Marco de invitación</p><div className="mt-3 grid grid-cols-3 gap-2">{frames.map((frame) => <button key={frame.id} onClick={() => set('invitationFrame', frame.id)} className={`relative h-24 overflow-hidden rounded-2xl border bg-[var(--event-secondary)] p-3 text-xs font-bold transition ${form.invitationFrame === frame.id ? 'border-[var(--event-primary)] text-[var(--event-primary)]' : 'border-slate-200 text-slate-600'}`}><EventFrame frame={frame.id} /><span className="relative">{form.invitationFrame === frame.id && <Check className="mx-auto mb-1 h-4 w-4" />}{frame.label}</span></button>)}</div></div><div className="mt-8"><p className="text-sm font-bold text-slate-700">Permitir aportes</p><div className="mt-3 flex flex-wrap gap-x-5 gap-y-3">{([['allowText', 'Texto'], ['allowImages', 'Fotos'], ['allowVideos', 'Videos'], ['allowAudio', 'Audio']] as const).map(([key, label]) => <label key={key} className="flex cursor-pointer items-center gap-2 text-sm font-semibold text-slate-600"><input type="checkbox" checked={form[key]} onChange={(e) => set(key, e.target.checked)} className="h-4 w-4 accent-[var(--event-primary)]" />{label}</label>)}</div></div><Button className="mt-9" onClick={save} isLoading={saving}>Guardar cambios</Button></section><aside className="rounded-[2rem] bg-[var(--event-mural)] p-5 text-white shadow-xl"><p className="text-xs font-extrabold uppercase tracking-[.18em] text-white/55">Vista previa</p><div className="mt-5"><div className="relative overflow-hidden rounded-3xl bg-white p-6 text-center"><EventFrame frame={form.invitationFrame} /><p className="relative text-xs font-extrabold uppercase tracking-widest text-[var(--event-primary)]">Estás invitado</p><p className="relative mt-3 text-xl font-extrabold text-[var(--event-ink)]">{form.name || 'Tu evento'}</p><p className="relative mt-4 text-xs text-slate-500">{form.welcomeMessageText || 'Tu mensaje de bienvenida aparecerá aquí.'}</p></div></div>{eventId && <div className="mt-6 space-y-2"><Button variant={event?.status === 'active' ? 'destructive' : 'secondary'} className="w-full" isLoading={acting} onClick={toggleStatus}>{event?.status === 'active' ? <><Square className="mr-2 h-4 w-4" />Cerrar evento</> : <><Play className="mr-2 h-4 w-4" />Activar evento</>}</Button><Button variant="outline" className="w-full" onClick={copy}><Copy className="mr-2 h-4 w-4" />Copiar código</Button>{event?.status === 'closed' && <Link to={`/admin/events/${eventId}/gallery`}><Button className="w-full"><ExternalLink className="mr-2 h-4 w-4" />Ver mural</Button></Link>}</div>}</aside></div></section></PageTransition></PageShell>
}
export default AdminEventPage
